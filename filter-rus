// This is a comment

line word	"..."  "…"

// artefacts
line regex	"\\s+(?:['’\"`,›]+)$"	""
line regex	`[—_-]{2,3}`			"—"
line regex	`(\pL)—$`				"${1}-"
line regex	`(\pL)—(\pL)`			"${1}-${2}"

// page numbers
text regex `\n{2,}\d{1,3}-?\n+`	"\n\n"

// words join
text regex `(\pL)-(?:\n+)`	"${1}"

